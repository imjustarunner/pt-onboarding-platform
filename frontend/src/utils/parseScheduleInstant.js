/**
 * Parse schedule event instants from API (UTC storage contract).
 * Re-exported for backward compatibility — see scheduleEventInstants.js.
 */
export { parseScheduleUtcInstant as parseScheduleInstant } from './scheduleEventInstants.js';
