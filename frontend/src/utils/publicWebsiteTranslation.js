// Translation adapter for public marketing copy, including content supplied by the page editor.
// Keeps each English text node intact as the source; never changes HTML, input values, URLs or editor controls.
export const WEBSITE_SPANISH = {
 'Home':'Inicio','About':'Acerca de nosotros','About Us':'Acerca de nosotros','Services':'Servicios','Our Services':'Nuestros servicios','Our Programs':'Nuestros programas','Programs':'Programas','Get Involved':'Participa','Impact':'Impacto','Our Impact':'Nuestro impacto','Resources':'Recursos','Contact':'Contacto','Contact Us':'Contáctanos','Our Network':'Nuestra red','Our providers':'Nuestros profesionales','Our tutors':'Nuestros tutores','Our coaches':'Nuestros coaches','Find a provider':'Encuentra un profesional','Find Providers':'Buscar profesionales','Find Support':'Encuentra apoyo','Get started':'Comenzar','Get Started':'Comenzar','Learn More':'Más información','Learn more':'Más información','Donate':'Donar','Donate Today':'Dona hoy','Donate Now':'Donar ahora','Support Our Mission':'Apoya nuestra misión','Support our mission':'Apoya nuestra misión','Privacy':'Privacidad','Privacy Policy':'Política de privacidad','Terms':'Términos','Terms of Service':'Términos de servicio','Coming soon':'Próximamente','Menu':'Menú','Close':'Cerrar','Menu ☰':'Menú ☰','Close ×':'Cerrar ×','Search':'Buscar','Search the website':'Buscar en el sitio','Skip to content':'Saltar al contenido','Partner With Us':'Colabora con nosotros','Partner with us':'Colabora con nosotros','Our partners':'Nuestros colaboradores','View all':'Ver todo','All services':'Todos los servicios','All partners':'Todos los colaboradores','Who We Help':'A quiénes ayudamos','Who We Support':'A quiénes apoyamos','Men':'Hombres','Boys':'Niños','Athletes':'Deportistas','Build Inner':'Desarrolla tu','Strength':'fortaleza interior','How It Works':'Cómo funciona','Contact our team':'Contacta a nuestro equipo','Begin intake →':'Iniciar inscripción →','Get started →':'Comenzar →','Real connections.':'Conexiones reales.','Brighter paths.':'Caminos más prometedores.','REAL CONNECTIONS.':'CONEXIONES REALES.','BRIGHTER PATHS.':'CAMINOS MÁS PROMETEDORES.','Our programs':'Nuestros programas','About MH4Kidz':'Acerca de MH4Kidz','The Unplugged Series':'The Unplugged Series','Real People.':'Personas reales.','Brighter Tomorrows.':'Mañanas más prometedores.','Expanding':'Ampliando','Mental Health':'la salud mental','Together.':'juntos.','A Shared Mission.':'Una misión compartida.','A Greater Reach.':'Un mayor alcance.','Different Strengths.':'Fortalezas distintas.','One Shared Mission.':'Una misión compartida.','Stronger Together':'Más fuertes juntos','Find the Right Support.':'Encuentra el apoyo adecuado.','Build. Manage. Scale.':'Construye. Gestiona. Crece.','Your Next Chapter.':'Tu próximo capítulo.','Let’s Get Your':'Pongamos tu','Business Moving.':'negocio en marcha.','Let’s Talk':'Hablemos','Let\'s Talk':'Hablemos','Choose Your Path':'Elige tu camino','Our story':'Nuestra historia','Our mission':'Nuestra misión','Counseling':'Terapia','Life Coaching':'Coaching de vida','Tutoring':'Tutoría','Mental health':'Salud mental','Mental Health Agencies':'Agencias de salud mental','Consultants':'Consultores','Coaches':'Coaches','Life Coaches':'Coaches de vida','Tutors':'Tutores','Other Service Businesses':'Otras empresas de servicios','Website search':'Búsqueda del sitio','Try again':'Intentar de nuevo','MH4Kidz':'MH4Kidz','Rise Revive':'Rise Revive','Plot Twist Co.':'Plot Twist Co.','Plot Twist Co':'Plot Twist Co','Plot Twist HQ':'Plot Twist HQ','Mental Range Collective':'Mental Range Collective','Inner Strength Institute':'Inner Strength Institute'
};
Object.assign(WEBSITE_SPANISH, {
 'Our Providers':'Nuestros profesionales','For Schools':'Para escuelas','For Families':'Para familias','School Services':'Servicios escolares','Careers':'Empleo','Client Portal':'Portal del cliente','Our Team':'Nuestro equipo',
 '← Back to website':'← Volver al sitio web','← Back to providers':'← Volver a profesionales',"I'm a parent":'Soy madre, padre o tutor','Start school enrollment →':'Iniciar inscripción escolar →','Ask about enrollment →':'Consultar sobre la inscripción →',
 'All providers':'Todos los profesionales','In-office providers':'Profesionales en consultorio','School-based providers':'Profesionales en escuelas','School':'Escuela','All schools':'Todas las escuelas','Client age':'Edad del cliente','All ages':'Todas las edades','Specialty':'Especialidad','All specialties':'Todas las especialidades','Insurance':'Seguro','All insurance':'Todos los seguros','New clients':'Nuevos clientes','Any status':'Cualquier estado','Accepting new clients':'Acepta nuevos clientes','Waitlist':'Lista de espera','Openings':'Horarios disponibles','Any availability':'Cualquier disponibilidad','With openings':'Con horarios disponibles','No published openings':'Sin horarios publicados','Clear filters':'Borrar filtros','Sort by':'Ordenar por','Name':'Nombre','Earliest online availability':'Primera disponibilidad en línea','Name, specialty, language…':'Nombre, especialidad, idioma…','Search schools…':'Buscar escuelas…',
 'Accepting · office & school':'Acepta clientes · consultorio y escuela','Accepting · office':'Acepta clientes · consultorio','Accepting · school':'Acepta clientes · escuela','Accepting · virtual':'Acepta clientes · virtual','Waitlist / contact our team':'Lista de espera / contacta a nuestro equipo','Not accepting':'No acepta nuevos clientes','Assigned schools ·':'Escuelas asignadas ·','Office ·':'Consultorio ·',
 'View profile':'Ver perfil','View times':'Ver horarios','Ask about waitlist':'Consultar lista de espera','View availability & full profile →':'Ver disponibilidad y perfil completo →','Find school enrollment →':'Buscar inscripción escolar →','Ask our team for help →':'Pide ayuda a nuestro equipo →','Your next step':'Tu próximo paso','Meet your provider':'Conoce a tu profesional','School locations':'Ubicaciones escolares','Specialties':'Especialidades','Populations served':'Poblaciones atendidas','Therapy approaches':'Enfoques terapéuticos','Languages':'Idiomas','Session formats':'Modalidades de sesión','In-person/virtual':'Presencial/virtual','English':'Inglés','Spanish':'Español','Contact our team for details.':'Contacta a nuestro equipo para obtener más información.',
 'Elementary Schools':'Escuelas primarias','Middle Schools':'Escuelas intermedias','High Schools':'Escuelas secundarias','Other Schools':'Otras escuelas','Find your school':'Encuentra tu escuela','Our school partnerships':'Nuestras alianzas escolares','Investing in students. Supporting schools.':'Invertimos en estudiantes. Apoyamos a las escuelas.','Explore the schools with ITSCO portal accounts and meet their assigned providers.':'Explora las escuelas asociadas con ITSCO y conoce a sus profesionales asignados.',
 'Pause insurance rotation':'Pausar rotación de seguros','Resume insurance rotation':'Reanudar rotación de seguros','Pause rotation':'Pausar rotación','Resume rotation':'Reanudar rotación',
 'Initial Interest Form':'Formulario de interés inicial','Client Enrollment Packet':'Paquete de inscripción del cliente','Start Form →':'Comenzar formulario →','Start Full Enrollment Packet →':'Comenzar inscripción completa →','A short form to get you started.':'Un formulario breve para comenzar.','A comprehensive intake experience.':'Una inscripción completa.','Need Help?':'¿Necesitas ayuda?',"We're here for you.":'Estamos aquí para ayudarte.','Send Us a Message':'Envíanos un mensaje','Basic contact information':'Información básica de contacto','Reason for seeking support':'Motivo de la consulta','Preferred communication method':'Método de comunicación preferido','All basic information':'Toda la información básica','Detailed history & concerns':'Historial detallado e inquietudes','Documents & signatures':'Documentos y firmas',
 'Selecting an eligible weekly time starts an intake hold. It does not book an appointment. The team confirms your placement.':'Seleccionar un horario semanal disponible lo retiene durante la inscripción. No confirma una cita. El equipo confirma tu asignación.'
});
const ROOTS = '.audience-site,.nlu-site,.itsco-site,.tisi-site,.rise-site,.mh-site,.ptco-site,.range-site,.pmh-sub-page,.pmh-page,.pmh-sub,.ajl,.ai-join-hub,.cr,.provider-profile,.prac-finder,.team-finder,.choose-provider-page';
const SKIP = 'script,style,svg,textarea,input,[contenteditable="true"],[translate="no"],.notranslate,.page-editor,.tisi-editor-bar,[class*="-ed-"],.public-translate-widget,.cr-editor-bar,.cr-photo-picker,.tf-editor-bar,.ajl--design,.public-profile-editor,.availability-settings,.website-editor-bar,.website-management-link';
// Content-addressed cache: edited English copy naturally gets a new key. Public copy only;
// form values, editable fields and authenticated management controls are never collected.
const CACHE_KEY = 'public-website-spanish:v2';
export function createWebsiteTranslator({ document, translate, onState = () => {}, storage }) {
 let saved = {};
 try { storage ??= document.defaultView?.localStorage; saved = JSON.parse(storage?.getItem(CACHE_KEY) || '{}'); } catch { saved = {}; }
 if (!saved || typeof saved !== 'object' || Array.isArray(saved)) saved = {};
 const entries = new Map(), attributes = new Map();
 const cache = new Map(Object.entries(saved).filter(([k,v])=>k.length<=8000 && typeof v==='string' && v.length<=16000));
 for (const [key,value] of Object.entries(WEBSITE_SPANISH)) cache.set(key,value);
 const attempted = new Set(), concealed = new Map();
 let spanish=false, stopped=false, timer, pending=false, failed=false;
 const rootFor=node=>node.parentElement?.closest(ROOTS);
 function persist() {
  try { storage?.setItem(CACHE_KEY,JSON.stringify(Object.fromEntries([...cache].slice(-3000)))); } catch { /* Private browsing/full storage: server cache still works. */ }
 }
 function collect() {
  for(const [node] of entries) if(!node.isConnected) entries.delete(node);
  for(const [element] of attributes) if(!element.isConnected) attributes.delete(element);
  for(const root of document.querySelectorAll(ROOTS)) {
   for(const element of root.querySelectorAll('[placeholder],[aria-label],[title]')) {
    if(element.closest(SKIP.replace('textarea,input,',''))) continue;
    const values=attributes.get(element)||{};
    for(const key of ['placeholder','aria-label','title']) {
     const value=element.getAttribute(key);
     if(value && /[A-Za-z]/.test(value) && (!values[key] || value!==values[key].rendered)) values[key]={source:value,rendered:value};
    }
    attributes.set(element,values);
   }
   const walker=document.createTreeWalker(root,4); let node;
   while((node=walker.nextNode())) {
    if(node.parentElement?.closest(SKIP) || !/[A-Za-z]/.test(node.data) || !node.data.trim()) continue;
    const option=node.parentElement?.closest('option');
    if(option && !option.hasAttribute('value')) option.setAttribute('value',option.textContent);
    const prior=entries.get(node);
    if(!prior || node.data!==prior.rendered) entries.set(node,{source:node.data,rendered:node.data});
   }
  }
 }
 const sources=()=>[...new Set([...entries.values(),...[...attributes.values()].flatMap(Object.values)].map(e=>e.source.trim()))];
 const missing=()=>sources().filter(s=>!cache.has(s) && !attempted.has(s) && s.length<=8000);
 function conceal(loading) {
  // Reveal the whole translated page together, including its nav and footer. Preserve layout
  // and prior accessibility/style values; never remove Vue nodes or recreate form controls.
  if(loading) for(const root of document.querySelectorAll(ROOTS)) {
   if(concealed.has(root)) continue;
   concealed.set(root,{visibility:root.style.visibility,busy:root.getAttribute('aria-busy')});
   root.style.visibility='hidden'; root.setAttribute('aria-busy','true');
  }
  else { for(const [root,prior] of concealed) {
   root.style.visibility=prior.visibility;
   if(prior.busy===null) root.removeAttribute('aria-busy'); else root.setAttribute('aria-busy',prior.busy);
  } concealed.clear(); }
 }
 function render() {
  observer.disconnect(); collect();
  for(const [node,entry] of entries) {
   if(!rootFor(node)) continue;
   const key=entry.source.trim(), translated=spanish?cache.get(key):null;
   const text=translated?entry.source.replace(key,translated):entry.source;
   if(node.data!==text) node.data=text;
   entry.rendered=text;
  }
  for(const [element,values] of attributes) for(const [key,entry] of Object.entries(values)) {
   const text=spanish?(cache.get(entry.source.trim())||entry.source):entry.source;
   if(element.getAttribute(key)!==text) element.setAttribute(key,text);
   entry.rendered=text;
  }
  conceal(spanish && (pending || missing().length>0));
  if(!stopped) observer.observe(document.body,{childList:true,subtree:true,characterData:true,attributes:true,attributeFilter:['placeholder','aria-label','title']});
 }
 async function run() {
  if(stopped || !spanish || pending) return;
  collect(); const strings=missing();
  if(!strings.length) { render(); onState(failed?'unavailable':''); return; }
  pending=true; onState('loading'); render();
  try {
   // A single page request normally covers all copy. No eight-string redraws.
   for(let i=0;i<strings.length;i+=500) {
    const batch=strings.slice(i,i+500); batch.forEach(s=>attempted.add(s));
    const response=await translate(batch);
    for(const s of batch) {
     const value=response?.translations?.[s];
     if(typeof value==='string' && value.trim() && (value!==s || response.configured===true)) cache.set(s,value);
     else failed=true;
    }
    if(stopped || !spanish) break;
   }
   persist();
  } catch { failed=true; }
  finally {
   pending=false;
   if(!stopped) {render(); onState(spanish?(failed?'unavailable':''):''); schedule();}
  }
 }
 function schedule() {clearTimeout(timer);timer=setTimeout(run,80);}
 const observer=new MutationObserver(()=>{render();schedule();});
 return {
  setSpanish(value) {spanish=!!value; if(!spanish) onState(''); render(); if(spanish && missing().length) onState('loading'); schedule();},
  refresh() {render();schedule();},
  retry() {attempted.clear();failed=false;render();schedule();},
  stop() {stopped=true;clearTimeout(timer);observer.disconnect();spanish=false;render();entries.clear();attributes.clear();}
 };
}
