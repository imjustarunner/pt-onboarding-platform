-- Migration 1284: Provider Update job description acknowledgment addendum (seed note)
-- Run after deploy: node backend/src/seeds/syncItscoContractLibrary.js
-- Adds JOB_DESC_ACK_ADDENDUM clause and itsco_job_description_acknowledgment_addendum config.

SELECT 1;
