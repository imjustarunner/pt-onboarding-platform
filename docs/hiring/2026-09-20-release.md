# Hiring and onboarding release — September 20, 2026

The user authorized committing and pushing the accumulated hiring, interview, pre-hire, and onboarding work on September 20. This supersedes the earlier local-only instructions recorded in the review notes.

Includes application receipts and document links, selectable availability, background applicant preparation, interview questions/invitations/calendar links and candidate-only meeting closure, supervisor eligibility, separate setup wizards, contract preview/token handling, PDF form support, portal fixes, and the shared clinical profile onboarding step.

The release excludes concurrent library/letterhead work, school-intake draft/reminder changes, and unrelated design assets and operational files. Shared application receipt changes in the intake controller were staged separately from the school-intake changes.

Validation used a separate export of the exact Git index: 159 backend hiring/clinical tests, 39 frontend tests, and 6 completed-intake record tests passed (204 total). Backend JavaScript syntax checks and the frontend production build passed. Public media copying was disabled only for the local build; deployment configuration is unchanged.

Rollout prerequisite: apply `database/migrations/1470_hiring_automated_assessment_author.sql` before testing automatic applicant assessments. It permits a null author for system-generated reports without changing existing attribution. This release preparation did not execute production migrations. The push to main triggers the existing frontend and backend deployment workflows.

After deployment, test a new application and receipt, applicant preparation, interview delivery/calendar import on real devices, then pre-hire and onboarding using the tenant portal. Regenerate malformed unsigned legacy agreements with the corrected preview flow. Confirm retained files exist and the handbook's external sharing permits candidate access; these data conditions cannot be repaired by the code release alone.
